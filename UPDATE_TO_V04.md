# دخل و خرج — v1.1.0 beta 1

این بسته روی سورس فعلی `main` پروژه KharjYar آماده شده و نام نمایشی برنامه را به «دخل و خرج» تغییر می‌دهد، بدون تغییر `applicationId` یا نام دیتابیس؛ بنابراین مسیر ارتقای داده‌های قبلی حفظ می‌شود.

## تغییرات اصلی

- نام و برند جدید «دخل و خرج» + لوگوی Vector و Adaptive Icon
- Splash سه‌ثانیه‌ای با انیمیشن ورود لوگو
- انیمیشن جابه‌جایی تب‌ها و باز/بسته شدن بخش‌های تنظیمات
- رنگ‌های آبی و سبز پررنگ‌تر
- لیست مشترک تراکنش/بدهی/قرض با مرتب‌سازی تاریخ یا مبلغ، افزایشی/کاهشی
- تغییر نام «ماه انتخابی» به «ماه»
- کارت گرافیکی و مرکزی مقایسه درآمد/هزینه ماه جاری
- انتخاب ساعت و دقیقه برای یادآورها
- Notification Channel جدید با صدای پیش‌فرض دستگاه و لرزش
- امکان ورود به تنظیم صدای کانال یادآورها
- لغو Alarm هنگام حذف قسط
- جلوگیری از تکراری‌شدن تراکنش‌های recurring با جدول occurrence یکتا
- ذخیره ساعت/دقیقه یادآور اقساط و بازیابی صحیح بعد از reboot
- مسیر Notification Listener به‌عنوان راه اصلی دریافت پیام بانکی، SMS به‌عنوان گزینه جانبی
- اصلاح parser بانکی برای ترجیح مبلغ تراکنش نسبت به موجودی حساب
- fail-safe شدن launcherهای Backup/Restore/PDF/Excel/SMS تا ActivityNotFound باعث بسته‌شدن اپ نشود
- PIN جدید با PBKDF2-HMAC-SHA256 + salt؛ PINهای قدیمی همچنان برای migration قابل ورود هستند
- غیرفعال شدن Android Auto Backup برای کاهش ریسک نشت داده مالی/credential
- حذف keystore و پسورد ثابت از سورس
- Release signing فقط از Gradle properties / environment / GitHub Secrets
- اضافه شدن unit test برای parser پیام بانکی
- بخش «درباره برنامه» و لینک GitHub/Issues

## GitHub Secrets لازم برای Release

یک keystore جدید بسازید و کلید قبلی موجود در تاریخچه repository را compromised در نظر بگیرید. سپس این Secrets را در GitHub تنظیم کنید:

- `DAKHL_KHARJ_KEYSTORE_BASE64`
- `DAKHL_KHARJ_STORE_PASSWORD`
- `DAKHL_KHARJ_KEY_ALIAS`
- `DAKHL_KHARJ_KEY_PASSWORD`

Workflow انتشار بدون این Secrets عمداً متوقف می‌شود.

## نکته SMS

خواندن مستقیم SMS روی برخی نسخه‌های Android/روش‌های نصب محدود است. در UI جدید، Notification Access مسیر اصلی و SMS مسیر اختیاری است. پیام بانکی همچنان ابتدا وارد صندوق بررسی می‌شود و خودکار به دفتر مالی اضافه نمی‌شود.

## سازگاری نصب روی نسخه قبلی

نسخه beta1 عمداً همان `applicationId` قبلی (`com.example.kharjyar`) و همان signing key نسخه‌های قبلی GitHub را نگه می‌دارد تا APK به‌صورت Update نصب شود و دیتابیس موجود کاربر حفظ شود. `versionCode` به 5 افزایش یافته و دیتابیس با migration از نسخه 3 به 4 ارتقا پیدا می‌کند؛ نصب Update نباید تراکنش‌ها و تنظیمات قبلی را پاک کند.

> این signing key قبلاً در repository عمومی بوده و برای انتشار عمومی بلندمدت امن محسوب نمی‌شود. بعد از تثبیت نسخه و تهیه بکاپ/مهاجرت مناسب، باید برای استراتژی signing دائمی تصمیم جداگانه گرفته شود.
